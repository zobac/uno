#ifndef GAMEACTION_H
#define GAMEACTION_H

//enum gameaction defines actions triggered by a card being played
enum GAMEACTION
{
    DRAW,
    SKIP,
    REVERSE,
    DITCH,
    PASS,
    NONE
};
#endif // GAMEACTION_H
